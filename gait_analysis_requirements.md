# 動画・CSV連動 歩行／ステップ動作解析Webアプリ 要件定義書

**文書版:** 0.1  
**作成日:** 2026年9月11日  
**対象:** PCブラウザ上で動作する研究用解析アプリケーション

## 1. 目的

OpenCap等から出力された関節角度CSVと同期動画を読み込み、動画を観察しながら歩行イベントまたはステップイベントを手動登録し、以下を算出・表示・出力する。

- 左右下腿遠位端間の前後方向位置差（以下、本アプリにおける「歩幅」）
- 右・左IC時の歩幅
- 歩幅の左右最大値、平均値、最小値、標準偏差および左右差
- 歩行周期および各歩行相の時間・割合
- ケイデンス
- 股関節・膝関節角度波形
- ステップ動作の歩幅および所要時間
- 測定距離が入力された場合の歩行速度

本アプリは、臨床判断を自動化するものではなく、検者が動画から判断したイベントに基づいて定量値を算出する研究支援ツールとする。

## 2. 利用対象・利用環境

- 主対象者：研究者、理学療法士等
- 主な使用端末：PC
- 正式対応ブラウザ：Google Chrome最新版、Microsoft Edge最新版
- 動作確認対象：Firefox最新版
- 動作保証対象外：Safari、Internet Explorer、スマートフォン・タブレット
- 利用時のインターネット接続：必須でも可
- 動画とCSVの解析：原則としてブラウザ内で実行し、外部サーバーへアップロードしない
- 解析内容のブラウザへの自動保存：行わない

## 3. 用語・符号規約

| 用語 | 定義 |
|---|---|
| Rt_IC | 右足部の初期接地 |
| Lt_IC | 左足部の初期接地 |
| Rt_Off | 右足部の足部離床 |
| Lt_Off | 左足部の足部離床 |
| IC | 対象足部のいずれかが床面へ最初に接触したフレーム |
| Off | 対象足部が床面から完全に離れた最初のフレーム |
| 歩幅 | 左右下腿遠位端間の前後方向位置差。一般的な踵間距離とは異なる |
| 元の符号付き歩幅 | 左下腿遠位端が右下腿遠位端より前方にある場合を正とする |
| IC側基準歩幅 | ICした足が反対側より前方にある場合を正とする |
| 絶対歩幅 | 元の符号付き歩幅の絶対値 |

関節角度は、CSVの定義を確認したうえで、原則として股関節屈曲を正、膝関節屈曲を正とする。CSVの符号規約が異なる場合は、解析条件として明示する。

## 4. 入力要件

### 4.1 動画

- 必須形式：MP4
- 推奨コーデック：H.264
- 推奨フレームレート：固定30 fps
- 可変フレームレート動画ではフレーム番号が推定値となることを表示する
- 推奨容量：500 MB以下
- 推奨時間：30分以下
- 動画ファイル名に個人情報が含まれることはシステム上制限しない
- 動画の幅、高さ、再生時間、取得可能な場合はフレームレートを表示する

### 4.2 CSV

- 文字コード：UTF-8、UTF-8 BOM付き、Shift_JISを候補とし、自動判定または選択可能とする
- 区切り文字：カンマ
- 推奨容量：100 MB以下
- 推奨行数：100万行以下
- 時刻列、関節角度列、セグメント角度列を列名から自動認識する
- 自動認識できない場合は、プルダウンで手動割り当てできる
- 欠損値、非数値、時刻の逆転、重複時刻、必要列不足を検出して表示する
- CSVの開始時刻、終了時刻、継続時間、行数、推定サンプリング周波数を表示する

### 4.3 身体計測値

- 大腿長
- 下腿長
- 骨盤幅
- 単位はcmまたはmを選択し、内部計算時に統一する
- CSVに値が存在する場合は取得し、存在しない場合は手入力できる
- 左右別の長さを利用する拡張が可能なデータ構造とする。初期版で左右共通値を用いる場合は明示する

## 5. CSVデータの検証

解析開始前に以下を確認する。

- 必須列の有無
- 時刻列が単調増加しているか
- サンプリング間隔の中央値とばらつき
- 欠損値および連続欠損区間
- 角度・身体計測値の単位
- 想定範囲から著しく外れた値
- 左右列の割り当て
- 関節角度とセグメント角度の符号方向

重大な不備は解析を停止し、軽微な不備は警告を表示して利用者が継続可否を選択する。

## 6. 動画操作要件

- 再生・一時停止
- 再生速度変更（0.25、0.5、1.0倍を含む）
- 1フレーム進む／戻る
- 10フレーム進む／戻る
- シークバーによる時刻移動
- 現在の動画時刻の表示
- 推定フレーム番号の表示
- 拡大・縮小
- 拡大時の上下左右移動（パン）
- 表示倍率・表示位置のリセット
- キーボードショートカット

推奨ショートカットは、Spaceを再生・一時停止、左右矢印を1フレーム移動、Shift＋左右矢印を10フレーム移動、＋／－を拡大・縮小、Rを表示リセットとする。入力欄へフォーカス中は誤操作を防止する。

## 7. 動画とCSVの同期

### 7.1 初期版の同期方法

動画全体の0～100％とCSV全体の0～100％を対応させる。

\[
t_{CSV}=\frac{t_{video}}{T_{video}}\times T_{CSV}
\]

### 7.2 同期情報の表示

- 動画時間
- CSV時間
- 両者の時間差
- 使用した同期方式
- 対応する動画時刻とCSV時刻

### 7.3 警告の暫定基準

- 0.2秒超または全体時間の2％超：時間比率補正を使用したことを表示
- 1.0秒超または全体時間の10％超：同期精度に関する強い警告

### 7.4 将来拡張

- 時間差なし同期
- 開始時刻のオフセット補正
- 動画とCSVの基準点を1点指定する同期
- 2点指定によるオフセット＋時間比率補正

初期画面では自動同期のみを提供してもよいが、内部データ上は同期方式を識別して保持する。

### 7.5 解析対象範囲

被験者が動画に映っていない区間は、イベント登録・解析の対象としない。検者は動画を目視しながらイベントを登録する運用のため、被験者が画面外の区間で同期精度が低下していても、MVPの機能要件上は問題とならない（Phase 0での実データ照合により確認）。

## 8. イベント登録要件

### 8.1 歩行イベント

- Rt_IC
- Lt_IC
- Rt_Off
- Lt_Off

### 8.2 登録機能

- 同じ種類のイベントを複数回登録できる
- 現在の動画時刻にイベントを登録できる
- 登録時に動画時刻、推定フレーム番号、対応CSV時刻を保存する
- 登録済みイベントを時間順に一覧表示する
- 登録済みイベントを修正・削除できる
- 動画タイムライン上にイベントマーカーを表示する
- 右・左およびIC・Offを色・形で区別する

### 8.3 重複・順序の扱い

- 同一フレームへの異なるイベント登録：許可
- 同一フレームへの同一イベント重複：警告し、原則として禁止
- イベント順序が標準的な歩行順序と一致しない場合：警告する
- 病的歩行を考慮し、警告後も利用者が確定できる
- イベント不足の周期：未完成周期として解析対象外にする

標準的なイベント順序は以下とする。

```text
Rt_IC → Lt_Off → Lt_IC → Rt_Off → 次のRt_IC
Lt_IC → Rt_Off → Rt_IC → Lt_Off → 次のLt_IC
```

## 9. 位置・歩幅の算出

### 9.1 セグメント位置

大腿長を \(L_T\)、下腿長を \(L_S\)、各セグメントの前後傾斜角度を \(\theta_T,\theta_S\) とし、CSVの角度定義に合わせて前後成分を算出する。

一般形は次のとおりとし、正負およびsin／cosの選択は座標系と角度ゼロの定義を検証データで確定する。

\[
x_{distal}=f(L_T,\theta_T)+f(L_S,\theta_S)
\]

- 右下肢距離：右股関節近位端に対する右下腿遠位端の前後位置
- 左下肢距離：左股関節近位端に対する左下腿遠位端の前後位置
- 骨盤回旋補正：LowerBack.Single.Euler.xと骨盤幅から左右股関節近位端の前後差を算出

実装前に、既存ExcelのAB、AC、AD、AE、AM、AN、AP、AQ、AR列の式と数値を基準データとして照合する。

### 9.2 歩幅の出力

元の符号付き歩幅を \(S\) とする。

\[
S_{RtIC}=-S,\qquad S_{LtIC}=S
\]

各ICについて次を保持する。

- 元の符号付き歩幅
- IC側基準歩幅
- 絶対歩幅 \(|S|\)
- 骨盤補正なし歩幅
- 骨盤補正あり歩幅

### 9.3 要約統計

右・左別に以下を算出する。

- 件数
- 最大値
- 最小値
- 平均値
- 標本標準偏差（分母 \(n-1\)）

1歩のみの場合、標準偏差は`NA`とする。

左右差は以下の2種類を出力する。

\[
\text{符号付き左右差}=\text{右歩幅平均}-\text{左歩幅平均}
\]

\[
\text{左右差絶対値}=|\text{右歩幅平均}-\text{左歩幅平均}|
\]

## 10. CSV値の時間補間

動画イベント時刻に対応する値は、前後のCSVデータを用いて線形補間する。

\[
x(t)=x_1+\frac{t-t_1}{t_2-t_1}(x_2-x_1)
\]

対象には関節角度、セグメント角度、左右下腿遠位端位置、骨盤補正値、歩幅を含む。補間値であること、前後のCSV時刻および対応行を結果に保存する。補間は20 Hzを超える実測情報を生成するものではないことを利用者へ説明する。

## 11. 歩行時間パラメータ

### 11.1 右下肢

| 指標 | 区間 |
|---|---|
| 一歩行周期時間 | Rt_ICから次のRt_IC |
| 立脚時間 | Rt_ICからRt_Off |
| 遊脚時間 | Rt_Offから次のRt_IC |
| 荷重応答期 | Rt_ICからLt_Off |
| 単脚支持期 | Lt_OffからLt_IC |
| 前遊脚期 | Lt_ICからRt_Off |

### 11.2 左下肢

右・左を入れ替えて同様に算出する。

各指標について秒と一歩行周期に対する割合（％）を出力する。両脚支持時間は、当該周期に含まれる荷重応答期と前遊脚期の合計とする。

### 11.3 ケイデンス

連続する左右IC間隔または解析区間内の歩数と時間から、1分当たり歩数を算出する。採用した計算方式を結果に記載する。

## 12. 関節角度波形

- 対象：左右股関節角度、左右膝関節角度
- 同側ICから次の同側ICまでを1歩行周期とする
- 各周期を0～100％の101点に線形補間する
- 個別周期波形を表示する
- 複数周期の平均波形を表示する
- 平均±標準偏差を表示できる
- 右・左を重ねて比較できる
- 元時間波形と正規化波形を切り替えられる
- グラフ画像および101点数値CSVを出力できる

## 13. 歩行速度

角度および身体部分長のみから床上での身体全体の移動速度は算出しない。初期版では、既知の測定距離と開始・終了イベントの時間差から算出する。

\[
\text{歩行速度}=\frac{\text{測定距離}}{\text{終了時刻}-\text{開始時刻}}
\]

- 測定距離の入力単位：m
- 開始・終了時刻：動画上で登録または数値入力
- 結果単位：m/s
- 測定距離または時刻が未入力の場合は算出しない

## 14. ステップ動作解析

### 14.1 登録項目

- ステップ側：右／左
- 動作開始
- ステップ側IC
- 必要に応じて動作終了

### 14.2 算出項目

- ステップ時間＝ステップ側IC－動作開始
- 動作完了時間＝動作終了－動作開始（動作終了登録時のみ）
- ステップ側IC時の元の符号付き歩幅
- ステップ側基準歩幅
- 絶対歩幅
- 骨盤補正なし／あり歩幅
- 複数試行の最大、最小、平均、標本標準偏差

## 15. 結果表示・出力

### 15.1 画面表示

- 入力ファイル情報
- 同期情報
- 登録イベント一覧
- IC時歩幅一覧
- 左右別の要約統計
- 歩行時間パラメータ
- 関節角度波形
- ステップ解析結果
- 警告・除外理由

### 15.2 出力ファイル

最低限、以下をCSVで出力する。

1. イベント一覧
2. 1歩行周期ごとの結果
3. 左右別の要約統計
4. 101点に正規化した関節角度波形
5. ステップ試行別結果

可能であれば、これらをZIPにまとめて出力する。

### 15.3 解析状態の保存

- ブラウザへの自動保存は行わない
- 任意操作により解析状態をJSONで保存できる
- 同じ動画とCSVを再選択したうえで、JSONを読み込み解析を再開できる
- 未保存状態で再読み込み・ファイル変更・画面終了を行う場合は警告する

## 16. 出力に含める再現性情報

- アプリ名・バージョン
- 解析日時
- 被験者ID、試行名（任意）
- 元動画名、元CSV名
- 動画時間、fps、推定総フレーム数
- CSV開始・終了時刻、サンプリング周波数、行数
- 列の割り当て
- 身体計測値と単位
- 角度の正負規約
- 同期方式と補正内容
- イベント時刻、推定フレーム番号、対応CSV時刻・行
- 線形補間の使用
- 骨盤補正の有無
- 警告および除外周期

## 17. 非機能要件

- ローカル解析中に動画・CSVを外部へ送信しない
- ファイル名は画面および出力結果に使用できる
- ファイル名が画面キャプチャやダウンロードフォルダに残る可能性を表示する
- 解析中の異常で画面全体が停止せず、利用者が原因を理解できるメッセージを表示する
- 大容量処理中は進捗または処理中表示を行う
- 計算結果は同じ入力・設定・イベントに対して再現可能である
- 計算ロジックとUIを分離し、自動テスト可能にする
- アクセシビリティを考慮し、色だけで左右・イベント種類を区別しない

## 18. 受入基準

- 基準CSVと既存Excelで、AB～AR列に対応する主要計算値が許容誤差内で一致する
- 既知時刻へ登録したイベントが、一覧・再読込後も同じ時刻として保持される
- 右・左の同一種類イベントを複数登録できる
- イベントを修正・削除できる
- 同一フレームの異なるイベントを保存できる
- 20 Hz CSVの中間時刻で線形補間値が手計算と一致する
- Rt_IC／Lt_IC時の符号変換が定義どおりである
- 各歩行相時間が手計算と一致する
- 同側IC間が0～100％の101点へ正規化される
- CSV・JSONをダウンロードできる
- ChromeおよびEdgeで主要機能が動作する
- 開発者ツールのネットワーク記録で、選択した動画・CSV本体が外部送信されない

## 19. 未確定事項

実装開始前または検証段階で以下を確定する。

- 既存Excelで用いられているセグメント前後成分の正確な式と角度ゼロ方向
- 骨盤補正あり歩幅の最終式
- 身体計測値の単位および左右別値の扱い
- CSV列名の全候補
- ケイデンスの主たる計算方式
- 同期警告の最終閾値
- ステップ動作の「動作開始」の観察基準
- 歩行速度の開始・終了イベント名称
- 数値表示の小数桁および許容誤差
- 推奨ファイル容量・時間の最終値

## 20. 初期版の優先範囲

### 必須（MVP）

- MP4・CSV読込と入力検証
- 動画操作、コマ送り、拡大・パン
- 4種類の歩行イベントの複数登録・修正・削除
- 自動同期と同期情報表示
- 線形補間
- IC時歩幅および左右要約統計
- 歩行相時間、ケイデンス
- 股・膝関節角度波形
- CSV出力
- JSON保存・再読込

### 第2段階

- ステップ動作解析
- 測定距離による歩行速度
- グラフ画像・ZIP出力
- 同期の手動補正
- イベント信頼度

